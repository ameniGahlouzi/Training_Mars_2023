#!/bin/bash

: 'script2.
1.donwload the file
2.decrypt and extract
3.check if docxker instale, if notDeepEqual, install it
4.check that the docker deksptop is running , if not running, start it
5.run the app through docker
'
